server:
  port: 7777
dubbo:
  application:
    name: dubbo-provider
  registry:
    address: zookeeper://127.0.0.1
    port: 2181
  protocol:
    name: dubbo
    port: 20880
  scan:
    base-packages: com.provider.system.service

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fm?useSSL=false&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 12345678
